Real customer photos only, and only with their permission.

Drop a square image here (400x400 is plenty), then set `photo` on that
entry in src/lib/site.ts, e.g.  photo: "/reviews/sarah-m.jpg"

If there's no photo, leave the field off — the initials circle renders
instead, which is what most credible local-business sites use.
